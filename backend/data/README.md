# Dataset Storage

- Raw datasets go inside the `data/raw/` folder.
- Datasets should **not** be committed to Git.
- `train_transaction.csv` and `train_identity.csv` will be placed in the `data/raw/` folder.
